{
    "extends": [
        "eslint:recommended",
        "plugin:svelte/recommended"
    ],
    "rules": {
        // override/add rules settings here, such as:
        // 'svelte/rule-name': 'error'
      }
}
