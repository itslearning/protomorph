/**
 * This is here only as example usage, and should not be used in a project.
 */

const baseConfig = require('./karma.conf.base');

module.exports = function (config) {
    config.set(baseConfig);
};
